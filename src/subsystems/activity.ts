/**
 * Activity session lifecycle (start / pause / resume / end).
 * Import: `react-native-fitness-geolocation/activity`
 */
export { ActivityManager, createActivityManager } from '../ActivityManager';
export { ActivityProfiles, getActivityProfile } from '../profiles/ActivityProfiles';
